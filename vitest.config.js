import { extractFromSvelteConfig } from 'vitest-svelte-kit';

// do not be fooled by the name of this file. Configurations are actually set in ./svelte.config.js in the `vite.test` object.

export default extractFromSvelteConfig();
